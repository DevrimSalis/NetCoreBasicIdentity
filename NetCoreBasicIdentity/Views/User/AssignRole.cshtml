@model RoleAssignSendModel

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<form asp-action="AssignRole" method="post" class="p-5 w-75 mx-auto shadow">
    <input type="hidden" asp-for="@Model.UserId"/>

    <div class="list-group">
        @for (int i = 0; i < Model.Roles.Count; i++)
        {
            <input type="hidden" asp-for="@Model.Roles[i].Name"/>
            <input type="hidden"asp-for="@Model.Roles[i].RoleId"/>
            <label class="list-group-item">
                <input class="form-check-input me-1" asp-for="@Model.Roles[i].Exist">
                @Model.Roles[i].Name
            </label>
        }
    </div>
    <button type="submit" class="btn btn-primary">Kaydet</button>

    <div class="text-danger mt-3" asp-validation-summary="ModelOnly">
    </div>
</form>